{
  "name": "systm-visual-tests",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "visual:run": "npx playwright test",
    "visual:run:headed": "npx playwright test --headed"
  },
  "devDependencies": {
    "@playwright/test": "^1.34.0"
  }
}
